{
  "scripts": {
    "sb:start": "supabase start -x realtime,storage-api,imgproxy,pgadmin-schema-diff,migra,logflare,vector,pgbouncer",
    "sb:stop": "supabase stop --no-backup"
  },
  "devDependencies": {
    "supabase": "^1.115.5"
  }
}
